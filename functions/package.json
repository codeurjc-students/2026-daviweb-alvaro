{
  "name": "functions-scripts",
  "main": "lib/index.js",
  "scripts": {
    "assign-owner": "bun scripts/assign-owner.ts",
    "upload-tenant-config": "bun scripts/upload-tenant-config.ts",
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": "20"
  }
}
